import expect from 'expect';
import MessageService from '../../app/services/MessageService';
import Thread from '../../app/models/Thread';
import Message from '../../app/models/Message';

describe('Message service tests', () => {

   /* it('add message saves message to db', async function(){
        let newMessage = new Message("hello world");
        let thread = new Thread('1111111111', false, null);
        let messageToBeSent = await MessageService.addMessage(thread, newMessage);
        expect(messageToBeSent).toBe(null);
    });
*/
});